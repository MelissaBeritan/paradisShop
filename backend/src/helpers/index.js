const {passwordValidator,confirmedPasswordValidator} = require('./passwordValidator');

//const emailValidator = require('./emailValidator');

module.exports = {
    passwordValidator,confirmedPasswordValidator
};